# 追加到厂商 CMake 工程（CubeMX / MCUXpresso 生成的主 CMakeLists.txt 末尾）

set(ZEPLOD_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/ThirdParty/zeplod-baremetal")
include(${ZEPLOD_ROOT}/cmake/zeplod.cmake)

zeplod_configure(
    ROOT    ${ZEPLOD_ROOT}
    PROFILE event
    BACKEND external
    CONFIG  ${CMAKE_CURRENT_SOURCE_DIR}/Core/Inc/bm_config.h
)

target_sources(${CMAKE_PROJECT_NAME} PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/Core/Src/bm_port.c
)
zeplod_link(${CMAKE_PROJECT_NAME})
