# 07 应用骨架与数据流

> **本文职责**：`main` 初始化顺序、示例数据流与常见排障。  
> **不负责**：重复 02–04 的原理说明。

从空白工程到跑起来：**`main` 写什么、数据怎么流**。机制接线见 [03](03-执行域与跨域通讯.md)、[04](04-核心机制.md)；示例列表见 [06](06-示例与上手路径.md)。

## 1. 三层结构

```text
应用：bm_config.h、模块表/控制实例表、main
框架：src/core、src/hybrid（CMake 链接 bm_core、bm_hrt 等）
HAL：`portable/` 或板级 Port（须设 `BM_BACKEND` 并链 `bm_backend_*`，勿只用弱符号桩）
```

---

## 2. 按层级选型

| 层级 | 组件 | 示例 |
|------|------|------|
| Nano | `bm_module` + `bm_event` | `core_sensor` |
| Nano+ | + `publish_copy_from_isr` | `interrupt_demo` |
| Lite | + `channel` / `shell` / `wdg` | `full_system` |
| Control | + `ctrl_inst` / `hrt` / `snapshot` / `sync` | `hrt_servo_stub` |

CMake 选项：[05-构建配置与CMake](05-构建配置与CMake.md)。

---

## 3. Nano 骨架：`core_sensor`

### 目录与模块声明

```text
core_sensor/
  main.c                 # 事件、主循环、业务数据流
  modules/
    mod_display.c        # BM_MODULE_DEFINE(display, ...)
    mod_logger.c
    mod_sensor.c
    module_table.c       # BM_MODULE_TABLE(...)
```

生命周期回调写在各 `mod_*.c`；`main.c` 只调 `bm_module_init_all()` / `start_all()`。宏说明见 [04 §4](04-核心机制.md#4-模块生命周期bm_module)。

### 初始化顺序

```c
bm_hal_uart_init(NULL);
bm_event_reset();
bm_event_register_type(...);
bm_event_subscribe(...);
bm_module_init_all();
bm_module_start_all();
for (;;) {
    /* 采样 + publish_copy */
    bm_event_process(8);
}
```

### 一周期数据流

```text
alloc → 填数据 → publish_copy → free 池对象
              →（异步入队）
bm_event_process → display_on_temp / logger_on_temp
```

事件接线详解：[04 §1](04-核心机制.md#1-事件系统bm_event)。

---

## 4. Lite 骨架：`full_system`

在 Nano 主循环上增加 `bm_shell_poll`、`bm_wdg_feed_module` + `bm_wdg_feed()`（全部模块 fed 后才喂硬件狗）。见 [04 §6](04-核心机制.md#6-软件看门狗bm_wdg)。

---

## 5. Control 骨架（混合域）

### 5.1 控制实例

每个伺服轴 / Pack 采样器一个 `bm_ctrl_inst_t`，槽位二选一：

| kind | 提供 |
|------|------|
| `BM_CTRL_SLOT_HARDWARE` | `step` + `bind_hardware` |
| `BM_CTRL_SLOT_SCHEDULED` | `step` + `period_us` |

Hardware / Scheduled 接线：[03 §3](03-执行域与跨域通讯.md#3-hardware-hrt-用法)、[03 §4](03-执行域与跨域通讯.md#4-scheduled-hrt-用法bm_hrt)。

### 5.2 初始化顺序

```c
bm_hal_timer_init(tick_hz);       /* 与 HRT/ticker 共用计数器 */
bm_ctrl_init_all(instances, n);   /* 组 HRT 表、bind Hardware、实例 init；会话 → INITED */
bm_sync_configure/arm(&domain);   /* 可选：可在 start_all 前完成 */
bm_ctrl_start_all(instances, n);  /* 全部 ops->start 后再 bm_hrt_start；会话 → STARTED */
bm_sync_trigger(&domain);         /* 可选：one-shot，重复须 bm_sync_arm */
bm_ticker_init(...);              /* 可选：subscribe 之后、定时器已 init */

for (;;) {
    bm_ticker_poll();
    bm_event_process();
    bm_wdg_feed();
}
```

| 常见错误 | 后果 |
|----------|------|
| 未调用 `bm_ctrl_start_all`（且无手动 `bm_hrt_start`） | Scheduled / ctrl 绑定的 Hardware `step` 不执行 |
| 在 `start_all` 前手动 `bm_hrt_start` 且经 `ctrl_inst` | 不推荐；会话门未开时 `step` 仍被忽略 |
| `sync_trigger` 后再次 trigger 未 re-arm | 返回 `BM_ERR_NOT_INIT` |
| `ticker_init` 在 `bm_hal_timer_init` 之前 | 返回 `BM_ERR_NOT_INIT` |
| 以为 `ticker_poll` 会调订阅者 | 必须再 `bm_event_process` |
| 仿真 Hardware 环无输出 | 须 `bm_hal_adc_sim_fire_complete` 或真机 IRQ |

### 5.3 运行时

```text
ISR:  Hardware 环（ADC 等） | Scheduled 环（bm_hrt tick）
主循环: ticker → event → wdg
```

---

## 6. 示例数据流（速查）

| 示例 | 数据流 |
|------|--------|
| `hrt_servo_stub` | ADC IRQ→`current_step`；TIM→`speed_step`；ticker→`EVENT_POSITION`→`process`；仿真用 `fire_complete` 模拟 ADC |
| `hrt_bms_coulomb` | Pack Hardware `SNAPSHOT_PUBLISH` 电流 → 电芯 Scheduled `READ` 积分 |
| `multi_channel_bms` | Pack 写 16 路 snapshot → ticker 事件 → SRT `READ` 过压判断 |
| `multi_axis_sync` | `sync_trigger` 后各轴 Scheduled `axis_step` 同 tick 记录 `start_tick` |
| `interrupt_demo` | `TIMER1_IRQHandler`→回调→`publish_from_isr`→`process` |

---

## 7. Nano → Control 升级清单

1. `bm_config.h` 增加 `BM_CONFIG_HRT_*`、`BM_CONFIG_MAX_CTRL_*`
2. CMake：`BM_ENABLE_HRT`、`BM_ENABLE_CTRL_INST`（多轴 + `BM_ENABLE_SYNC`）
3. 环算法迁入 `bm_ctrl_slot_t.step`
4. HRT→SRT 数据改 `bm_snapshot`；通知保留事件
5. `bm_ctrl_init_all` 替代手写 `bm_hrt_init`（多实例）

---

## 8. 排障

| 现象 | 查 |
|------|-----|
| Scheduled 不跑 | `bm_ctrl_start_all`（或 `bm_hrt_start`）、`period_us` 整除 `BM_CONFIG_HRT_TICK_US` |
| Hardware 不跑 | `bind` 返回值、HAL 是否强符号、仿真是否 `fire_complete` |
| 订阅不触发 | `register`/`subscribe`、`bm_event_process` |
| 多轴不同步 | sync 顺序、是否链 `bm_sync_hal_native` |
| 绑了没反应 | 是否只链 `bm_hal` 弱符号桩 → [08](08-HAL移植指南.md) |

测试：[09-测试与调试](09-测试与调试.md)。
